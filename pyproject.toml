[project]
name = "vim-speed-testing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
[tool.uv.extra-build-dependencies]
mmcv = ["setuptools"]
causal-conv1d = ["torch"]